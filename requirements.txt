# --- LLM / embeddings ---
anthropic>=0.49.0
openai>=1.50.0

# --- Market data / filings / news ---
yfinance>=0.2.40
finnhub-python>=2.4.20
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# --- Data wrangling ---
pandas>=2.2.0
numpy>=1.26.0

# --- Rendering ---
jinja2>=3.1.0

# --- RAG (LangChain + OpenAI embeddings + Chroma) ---
chromadb>=0.5.5
langchain>=1.0.0
langchain-openai>=0.3.0
langchain-chroma>=0.2.0
# optional: local cross-encoder reranker backend (RERANKER=cross_encoder); the
# default reranker is "llm" (Claude) and needs nothing extra.
sentence-transformers>=3.0.0

# --- API ---
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=2.6.0

# --- Infra ---
python-dotenv>=1.0.0
tenacity>=8.2.0

# --- Observability (optional: LLM tracing via Langfuse) ---
# Tracing no-ops unless LANGFUSE_PUBLIC_KEY + LANGFUSE_SECRET_KEY are set. OpenAI
# calls are traced via langfuse's drop-in client; Anthropic via the OTel instrumentor.
langfuse>=3.0.0
opentelemetry-instrumentation-anthropic>=0.30.0

# --- Dev ---
pytest>=8.0.0
